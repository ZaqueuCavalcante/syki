## ▶️ Introdução

Essa é a documentação da API do Exato Admin v2.

## 🔑 Autenticação

A autenticação é feita via JWT (enviado no X-Exato-Cookie).

Ao fazer login na API, o backend já seta o Cookie com o JWT, autenticando todas as chamadas posteriores.

Os enpoints marcados com o ícone 🔓 estão abertos.
