@using Syki.Front.Components.Custom
@using Syki.Front.Components.Buttons
@using Syki.Front.Features.Academic.GetCourses

@namespace Syki.Front.Features.Academic.CreateDiscipline

<MudDialog Class="pb-2">
    <TitleContent>
        <SykiDialogTitle Text="Nova Discipline" />
    </TitleContent>
    <DialogContent>
        <MudForm @ref="@_form">
            <SykiTextField Label="Nome" AutoFocus="true" @bind-Value="@_name" OnKeyDown="@Submit" />
            <MudSelect
                Dense="true"
                Margin="Margin.Dense"
                Variant="Variant.Outlined"
                Class="mb-2"
                T="CourseOut"
                Label="Cursos"
                MultiSelection="true"
                @bind-SelectedValues="@SelectedCursos"
                AnchorOrigin="Origin.BottomCenter"
            >
                @foreach (var curso in Cursos)
                {
                    <MudSelectItem T="CourseOut" Value="@curso">@curso.Name</MudSelectItem>
                }
            </MudSelect>
        </MudForm>
    </DialogContent>
    <DialogActions>
        <DialogCancelButton OnClick="@Cancel" />
        <SykiProgressCircular Loading="@_loading" />
        <DialogSaveButton OnClick="@Submit" />
    </DialogActions>
</MudDialog>

@inject ISnackbar Snackbar
@inject GetCoursesClient GetCoursesClient
@inject CreateDisciplineClient CreateDisciplineClient

@code
{
    [CascadingParameter]
    MudDialogInstance MudDialog { get; set; }

    MudForm _form;
    private bool _loading;
    private string _name;

    private List<CourseOut> Cursos = [];
    IEnumerable<CourseOut> SelectedCursos { get; set; } = new HashSet<CourseOut>() { };

    protected override async Task OnInitializedAsync()
    {
        Cursos = await GetCoursesClient.Get();
    }

    async Task Submit()
    {
        if (_loading) return;

        await _form.Validate();
        if (!_form.IsValid) return;

        _loading = true;
        var response = await CreateDisciplineClient.Create(_name, SelectedCursos.ToList().ConvertAll(c => c.Id));
        if (response.IsSuccessStatusCode)
        {
            MudDialog.Close(DialogResult.Ok(true));
            Snackbar.Add("Discipline cadastrada com sucesso!", Severity.Success);
        }
        else
        {
            var error = await response.ToError();
            Snackbar.Add(error.Message, Severity.Error);
        }
        _loading = false;
    }

    void Cancel() => MudDialog.Cancel();
}
